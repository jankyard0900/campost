<div class="border border-dark text-center">
  <b>エリア検索</b>
</div>
<div class="border border-dark">
  <% areas.each do |area| %>
    <p>
      <%= link_to camp_search_path(area) do %>
        <%= area.name %>
      <% end %>
    </p>
  <% end %>
</div>